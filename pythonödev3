def kontrol(str):
  count = 0
  for ch in str:
    if ch == '@gazi.edu.tr':
      count = count + 1
 
  if count == 1:
    return True
  else:
    return False
 
mail=input('mail : ')
if(kontrol(mail)==True):
      print('Doğru')
else:
      print('Yanlış')
